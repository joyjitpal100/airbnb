[build]
  command = "cd netlify/functions/vault && npm install"
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/vault/*"
  to = "/.netlify/functions/vault/:splat"
  status = 200
